export default class User {
    constructor(firstName, lastName, email, password, cpf) {
      this.firstName = firstName;
      this.lastName = lastName;
      this.email = email;
      this.password = password;
      this.cpf = cpf;
    }
  }